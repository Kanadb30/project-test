services:
  - type: web
    name: gnuplot-app
    env: python
    buildCommand: |
      apt-get update && apt-get install -y gnuplot
      pip install -r requirements.txt
    startCommand: python main.py